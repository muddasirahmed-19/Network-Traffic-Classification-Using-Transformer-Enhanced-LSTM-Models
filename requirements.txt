# Core ML / deep learning
# No strict version pins -- Google Colab already ships recent,
# mutually-compatible versions of these. Pinning older exact versions
# here previously caused a numpy/TensorFlow binary-incompatibility error
# on Colab (downgrading numpy broke TensorFlow's compiled C extensions).
# If you're running this OUTSIDE Colab (e.g. your own machine) and don't
# have these installed yet, plain `pip install -r requirements.txt` will
# just fetch the latest compatible versions of everything below.
tensorflow
scikit-learn
numpy
pandas
pyarrow      # needed if your dataset file is .parquet instead of .csv

# Plotting
matplotlib
seaborn

# Utilities
tqdm
joblib
